# 插件建设规则

## 目录

- 每建设一个浏览器插件，都必须在项目根目录下生成独立插件文件夹。
- 插件源码、说明、更新说明、聊天记录都放在对应插件文件夹内。
- 不同插件的文件和聊天记录必须分开，不能混放。

## 命名

- 插件名按功能命名，保持中性。
- 不直接使用示例网站、目标网站或临时测试网站命名。

## 版本

- 每次修改插件，都必须升级 `manifest.json` 里的 `version`。
- 小改动默认升级补丁号，例如 `1.0.1` 到 `1.0.2`。
- 根目录 `插件清单.md` 必须同步更新插件最新版本号。

## 更新说明

- 每个插件文件夹下必须有 `更新说明.md`。
- 每次版本升级，都在该文件顶部追加新版更新说明。
- 不覆盖旧版本更新说明。

## 聊天记录

- 每个插件文件夹下必须有独立的 `聊天记录.md`。
- 新版本生成或修改插件时，直接追加到同一个聊天记录文件。
- 不覆盖、不新建多个聊天记录文件。
- 不同浏览器插件的聊天记录必须保存在各自插件文件夹内，不能混淆。
