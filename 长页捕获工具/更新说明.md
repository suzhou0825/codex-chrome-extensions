# 更新说明

## 1.0.2

- 修复 PDF 合成后下载时可能出现 `atob` 解码失败的问题。
- 调整 PDF base64 分块编码逻辑，避免生成非法 base64 字符串。

## 1.0.1

- 修复 PDF 自动滚动截图时可能触发 Chrome `MAX_CAPTURE_VISIBLE_TAB_CALLS_PER_SECOND` 限制的问题。
- 增加截图接口调用节流，避免截图请求过快。

## 1.0.0

- 新增长页自动滚动截图功能。
- 支持保存长截图 PNG，页面过长时自动拆分为多张图片。
- 支持保存多页 PDF。
- 默认保存到浏览器下载文件夹。
