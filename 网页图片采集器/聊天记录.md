# 聊天记录

## 2026-06-11 版本 1.0.6

- 用户反馈另一个今日头条页面采集仍然偏少。
- 判断 1.0.5 的 `width <= 700` 条件仍会误杀正文竖图。
- 调整为仅过滤小方图和横版卡片缩略图，保留正文竖图和大图。
- 升级版本号到 `1.0.6`，同步更新插件清单和更新说明。

## 2026-06-11 版本 1.0.5

- 用户反馈新今日头条页面采集图片明显变少。
- 判断 1.0.4 对 `tplv-tt-post` 一刀切过滤过严，部分正文图也可能使用该格式。
- 改为结合图片尺寸过滤：仅跳过小尺寸或横版卡片缩略图，保留较大的正文图。
- 升级版本号到 `1.0.5`，同步更新插件清单和更新说明。

## 2026-06-11 版本 1.0.4

- 用户反馈仍采集到乱七八糟的图片，并提供 3 张误采样例。
- 样例文件名均包含 `tplv-tt-post`，判断为今日头条推荐/卡片缩略图形态。
- 新增今日头条 `tplv-tt-post` URL 过滤，减少正文下方推荐内容缩略图误采。
- 升级版本号到 `1.0.4`，同步更新插件清单和更新说明。

## 2026-06-11 版本 1.0.3

- 用户反馈仍采集到正文下方推荐文章图片，例如“超清女神壁纸（651）曾可妮”。
- 判断这类图片通常处于跳转到其他文章/新闻的链接卡片中。
- 新增文章卡片链接过滤：图片、source、背景图如果位于非图片链接且疑似跳转其他文章，则跳过。
- 升级版本号到 `1.0.3`，同步更新插件清单和更新说明。

## 2026-06-11 版本 1.0.2

- 用户反馈今日头条页面仍采集到主体内容之外的图片，例如附录图片。
- 用户提供了页面地址和误采样例图片。
- 尝试访问页面结构时遇到站点反爬脚本，无法直接取得完整渲染 DOM。
- 将正文根容器收窄为最高评分区域，并新增附录、相关阅读、推荐阅读、参考资料、声明、评论等标记之后的图片过滤。
- 升级版本号到 `1.0.2`，同步更新插件清单和更新说明。

## 2026-06-11 版本 1.0.1

- 用户要求只采集当前页面下的主题相关图片，不要采集图标和其他推荐新闻的图片。
- 将采集范围改为优先识别正文、文章、主内容区域。
- 增加排除规则，过滤导航、侧栏、推荐新闻、广告、评论、图标、头像等区域或图片。
- 默认最小图片宽度调整为 300 像素。
- 升级版本号到 `1.0.1`，同步更新插件清单和更新说明。

## 2026-06-11 版本 1.0.0

- 用户要求生成新的浏览器插件，点击后获取当前网页下面的所有图片并保存到下载文件夹。
- 用户要求尽量获取原图，而不是缩略图。
- 用户要求提取到的图片需要在下载文件夹里新建一个文件夹。
- 按项目规则创建独立插件文件夹 `网页图片采集器`。
- 实现图片提取、原图候选选择、背景图采集、批量下载到独立子文件夹。
